import  {Component} from "react";
import { Styled{{properCase name}} } from "./styled";

type Props = {
  children: React.ReactNode;
}

class {{ properCase name }} extends Component<Props, {}> {
  public render() {
    return (
      <Styled{{properCase name}}>
        {this.props.children}
      </Styled{{properCase name}}>
    );
  }
}

export default {{ properCase name }};
