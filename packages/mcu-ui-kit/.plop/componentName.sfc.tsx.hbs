import { memo,HTMLProps, ReactNode, FC } from "react";
import { Styled{{properCase name}} } from "./styled";

type {{properCase name}}Props = { color: string } 
 
const {{properCase name}}: FC<{{properCase name}}Props & HTMLProps<ReactNode>> = ({ children }) => (
<Styled{{properCase name}}>
  {children}
</Styled{{properCase name}}>
); 

export default memo({{properCase name}});