import { StyledButton } from './styled/buttonLeft';

const TopBar = () => {
  return (
    <div>
      <StyledButton />
      ...
      <button> {'>'} </button>
    </div>
  );
};

export default TopBar;
